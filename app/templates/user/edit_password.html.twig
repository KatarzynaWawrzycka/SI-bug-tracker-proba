{% extends 'base.html.twig' %}

{% block title %}{{ 'title.edit_password'|trans }}{% endblock %}

{% block body %}
    <h2>{{ 'title.edit_password'|trans }} — {{ user.email }}</h2>

    {{ form_start(form) }}
    {{ form_row(form.plainPassword) }}
    {{ form_row(form.confirmPassword) }}
    <button class="btn btn-primary">{{ 'action.save'|trans }}</button>
    {{ form_end(form) }}

    <a href="{{ path('user_show', {id: user.id}) }}" class="btn btn-secondary mt-3">
        {{ 'action.back'|trans }}
    </a>
{% endblock %}
